export const CHECK_UPDATE_TIMEOUT = 1000

export const SMALL_WIDTH  = (220 + 3 + 2)       // grid, grid-gap, borders
export const SMALL_HEIGHT = (225 + 4 + 2 + 80)  // grid, grid-gap, borders, display

export const BIG_WIDTH  =   (440 + 7)           // grid, grid-gap, borders
export const BIG_HEIGHT =   (225 + 4 + 2 + 80)  // grid, grid-gap, borders, display
